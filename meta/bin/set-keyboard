#! /usr/bin/sh
# Make this dynamic!
# I can get this using:
# thiago@voidao ~> setxkbmap -query | grep layout
# layout:     us
# thiago@voidao ~> setxkbmap -query | grep variant
# variant:    intl

set -e

setxkbmap -layout us -variant intl;
setxkbmap -option ctrl:nocaps;

# setxkbmap -model abnt2 -layout br -variant abnt2;
# setxkbmap -option ctrl:nocaps;

xset r rate 300 50;
